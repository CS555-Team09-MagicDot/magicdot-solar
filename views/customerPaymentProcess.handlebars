<div class="container mx-auto my-10">
  <h1 class="text-2xl font-bold mb-5">Payment Successful</h1>
  <div class="mt-10 border-t-2 border-gray-300 pt-5">
    <h2 class="text-lg font-bold mb-5">Payment Summary</h2>
    <div class="flex justify-between mb-3">
      <span>Total Amount:</span>
      <span class="font-bold" id="total-amount">{{totalAmount}}</span>
    </div>
    <div class="flex justify-between">
      <span>Payment Plan:</span>
      <span class="font-bold" id="payment-plan">{{paymentPlan}}</span>
    </div>
  </div>
</div>