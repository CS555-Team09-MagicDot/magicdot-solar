<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Project Details Page</title>
    <style>
      /* Style for the project details page */
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f1f1f1;
      }
      
      .container {
        width: 90%;
        max-width: 1200px;
        margin: 0 auto;
      }
      
      h1 {
        font-size: 36px;
        color: #333;
        margin-top: 40px;
        margin-bottom: 20px;
      }
      
      .project-details {
        background-color: #fff;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
      }
      
      .details-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px;
        border-bottom: 1px solid #ddd;
      }
      
      .details-item:last-child {
        border-bottom: none;
      }
      
      .details-item .label {
        font-weight: bold;
        color: #555;
      }
      
      .details-item .value {
        color: #333;
      }
      
      .btn {
        display: inline-block;
        padding: 10px 20px;
        background-color: #007bff;
        color: #fff;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s ease;
      }
      
      .btn:hover {
        background-color: #0062cc;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Project Details</h1>
      <div class="project-details">
        <div class="details-item">
          <span class="label">Project Name:</span>
          {{!-- <span class="value">Sample Project</span> --}}
          <span class="value">{{project.name}}</span>
        </div>
        <div class="details-item">
          <span class="label">Project ID:</span>
          {{!-- <span class="value">12345</span> --}}
          <span class="value">{{project._id}}</span>
        </div>
        <div class="details-item">
          <span class="label">Operational Manager:</span>
          {{!-- <span class="value">John Smith</span> --}}
          <span class="value">{{project.operationsManager}}</span>
        </div>
        <div class="details-item">
          <span class="label">Customer:</span>
          {{!-- <span class="value">ABC Company</span> --}}
          <span class="value">{{project.customer}}</span>
        </div>
        <div class="details-item">
          <span class="label">Start Date:</span>
          {{!-- <span class="value">2022-01-01</span> --}}
          <span class="value">{{project.startDate}}</span>
        </div>
        <div class="details-item">
          <span class="label">End Date:</span>
          {{!-- <span class="value">2022-06-30</span> --}}
          <span class="value">{{project.endDate}}</span>
        </div>
        <div class="details-item">
          <span class="label">Status:</span>
          {{!-- <span class="value">In Progress</span> --}}
          <span class="value">{{project.status}}</span>
        </div>
        {{!-- <div class="details-item">
          <span class="label">Budget:</span>
          <span class="value">TBD</span>
          <span class="value">{{project.budget}}</span>
        </div> --}}
        <a href="/onsiteTeam" class="btn">Back to Dashboard</a>
        </div>